target/
Cargo.lock.bak
*.rs.bk
# Derived from spec/openapi.yaml by `cargo xtask sync-spec`.
# Regenerate locally; source of truth is spec/openapi.yaml.
spec/openapi.normalized.yaml
.DS_Store
.env
.env.*
!.env.example
*.log
# CLI project-local config dir (contains secrets — mode 0600).
.taskfast/
# Legacy dotenv from pre-JSON-config builds. Left on disk after migration
# so users can audit before removing; never commit.
.taskfast-agent.env
